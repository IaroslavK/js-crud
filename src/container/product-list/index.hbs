{{!
Це код container файлу для index сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
}}

<a class="form__create_card" href="/product-create">Создать карточку товара</a>
<h1 class="page__title">Список товаров</h1>
<div class="card__field">
  {{#with data.products}}
  {{#if isEmpty}}
  <mark>Список пустой <a href="/product-create">Вернутся к созданию</a></mark>
  {{else}}
  {{#each list}}<div class="card">
    <h2 class="card__title">{{name}}</h2>
    <p class="card__description">{{description}}</p>
    <p class="card__id">ID: {{id}}</p>
    <div class="card__footer">
      <p class="card__product_price">{{price}}₴</p>
      <a class="card__product_edit" href="/product-edit?id={{id}}">Редактировать</a>
    </div>
  </div>
  {{/each}}
  {{/if}}
  {{/with}}
</div>
{{!
Цей код містить лише те, що буде виводитись в

<body>
  Адже наш layout/default.hbs має вже структуру HTML
  }}